# Core dependencies — Phase 1
requests>=2.28.0
httpx>=0.27.0
youtube-transcript-api>=0.6.0
python-dotenv>=1.0.0
fpdf2>=2.7.0

# Phase 2 (backend API)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Phase 3 (frontend) — not needed yet